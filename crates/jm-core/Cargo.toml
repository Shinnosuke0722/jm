[package]
name = "jm-core"
version = "0.1.0"
edition = "2021"
description = "Core types and logic for jm JDK version manager"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
thiserror = { workspace = true }
dirs = { workspace = true }
chrono = { workspace = true }
fs2 = "0.4"

[dev-dependencies]
tempfile = "3"
